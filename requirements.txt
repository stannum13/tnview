# Runtime dependencies.
#
# TNView currently has no required third-party runtime dependencies. Install the
# package itself with `python -m pip install -e .` or run `make setup` for the
# development environment.
